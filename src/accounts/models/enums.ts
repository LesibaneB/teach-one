export enum AccountTypes {
  INSTRUCTOR = 'instructor',
  STUDENT = 'student',
  ADMIN = 'admin',
}

export enum AccountStatus {
  INACTIVE = 'inactive',
  ACTIVE = 'active',
  DEACTIVATED = 'deactivated',
}
